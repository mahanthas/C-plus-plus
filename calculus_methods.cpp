#include<bits/stdc++.h>
using namespace std;

int main(){
    int x =10;
    int y = 20;
    int z ;

    //z = max(x,y);
    //z = min(x,y);
    //z = abs(-x);
    //z = sqrt(9);
    //z = pow(3,3);
    //z = round(3.14);
    //z = ceil(3.99);
    z = floor(3.99);

    cout<<z<<"\n";

    return 0;
}